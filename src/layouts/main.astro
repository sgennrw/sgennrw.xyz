---
import MetaTags, {
  Props as MetaProps,
} from "../components/core/MetaTags.astro";
import Navbar from "../components/core/Navbar.astro";
import "../styles/root.css";
const props = (Astro.props.frontmatter || Astro.props) as MetaProps;
---

<html lang="en">
  <head>
    <MetaTags {...props} />
  </head>
  <body>
    <Navbar />
    <slot />
  </body>
</html>
